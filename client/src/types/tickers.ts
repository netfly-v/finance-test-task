export interface DataType {
  key?: string;
  change: string;
  change_percent: string;
  exchange: string;
  last_trade_time: string;
  price: string;
  ticker: string;
  yield: string;
  isWatching?: boolean;
}
